export type TOrder = {
  email: string;
  productId: string;
  price: number;
  quantity: number;
};

// export interface Product extends Model<TStudent> {
//   isUserExists(id: string): Promise<TStudent | null>;
// }
